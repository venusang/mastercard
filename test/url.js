(function() {
  var assert;

  assert = chai.assert;

  describe('AP.utility.Url', function() {
    return describe('parseUrl()', function() {
      return it('should parse a URL into key/value pairs of bits and pieces', function() {});
    });
  });

}).call(this);
